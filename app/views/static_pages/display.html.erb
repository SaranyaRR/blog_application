
  <% @posts.each do |post| %>
  <tr>
    <td><%= post.title %> </td>
    <td><%= truncate post.content %></td>
     <td><%= post.category %> </td>
   <!-- <td><%= post.user_id %></td>-->
 </tr>
 <tr>
    <td>
       <%= time_ago_in_words post.created_at %> ago by <%= post.user.email %> 
    </td>
  </tr>
  <br/> 

 <tr></tr>
 <tr></tr>
 <tr>
    <td><%= link_to 'Read more..', post, class: "btn" %></td>
 </tr>

 <% end %>